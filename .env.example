# Safe template only. Never commit real credentials.
# Frontend (Vite) — set these in Vercel / Cloudflare Pages env vars too.

VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_BROWSER_SAFE_ANON_KEY
VITE_GOOGLE_MAPS_API_KEY=
VITE_OSRM_URL=https://router.project-osrm.org

# Server-only (local scripts / sync). Do NOT add these to the frontend host.
# SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=
